<h2>EDIT POST PAGE</h2>

<%= bootstrap_form_tag url: post_update_path(@post), method: :put do |f| %>
  <%= f.text_field :title, placeholder: @post.title %>
  <%= f.text_field :content, placeholder: @post.content %>
  <%= f.submit "Update Post" %>
<% end %>